# 프로젝트 정의서: 써주세요 4차 심사 자료 폼 내재화
> 작성일: 2026-04-28 / 작성자: 쮸리(Claude) for 댄디어빠쮸너야님
> 수신: 써주세요 2.0 서버팀 (Claude Code)
> 상태: **DRAFT** — API URL / 파일 업로드 방식은 서버팀 결정 후 확정 예정

---

## 1. 프로젝트 배경 및 목적

### 현재 상황
- 4차 심사 자료 수집을 외부 SaaS **Tally**(탈리)로 운영 중
- 탈리에 고객 금융정보 + 증빙서류 파일이 저장됨 → 데이터 주권 없음
- 써주세요 어드민에서 직접 조회/다운로드 불가
- 1차 신청 폼(`apply.html`)과 동일하게 써주세요 자체 인프라로 내재화 목표

### 목표
- 고객 금융정보(소득/지출/신용) + 증빙서류 파일을 써주세요 AWS에 직접 저장
- 관리자 어드민에서 4차 심사 데이터 조회 + 파일 다운로드
- 탈리 의존성 제거 → 월 6만원 비용 절감

---

## 2. 시스템 전체 구성도

```
[고객]
  ↓ 4차 심사 폼 작성 + 파일 업로드
[apply-4차.html] ← 프론트엔드 (쮸리팀 개발 예정)
  ↓ 텍스트/숫자 데이터 POST
  ↓ 파일 업로드 (방식은 서버팀 결정)
[API 서버] ← 서버팀 담당
  ├─ 텍스트 데이터 → [DB: review_applications]
  └─ 파일 → [S3 버킷]
       ↑ 조회 + 파일 다운로드
[어드민 패널] ← 써주세요 기존 어드민 (서버팀 기능 추가)
       ↑ 로그인
[관리자(댄디어빠쮸너야님)]
```

---

## 3. 수집 항목 전체 (DB 설계 및 API 설계용)

### 3-1. 기본정보

| 항목명 | 필드 설명 | 타입 | 필수 | 비고 |
|--------|----------|------|------|------|
| 성함 | 고객 이름 | 문자열 | ✅ | 한글 2~5자 |
| 생년월일 | 고객 생년월일 | 문자열 | ✅ | YYYY.MM.DD 형식 |
| 휴대폰번호 | 연락처 | 문자열 | ✅ | 01X 시작 10~11자리 |

### 3-2. 소득과 지출 (단위: 만 원, 모두 숫자)

| 항목명 (탈리 문구) | 필드 설명 | 타입 | 필수 |
|-------------------|----------|------|------|
| 연 소득 | 연간 소득 | 정수 | ✅ |
| 1년 중 월 최고 소득 | 최대 월소득 | 정수 | ✅ |
| 월 고정 소득 | 고정 월소득 | 정수 | ✅ |
| 1년 중 월 최고 지출 | 최대 월지출 | 정수 | ✅ |
| 월 고정 지출 | 고정 월지출 | 정수 | ✅ |
| 신용카드 사용 여부 | 사용 여부 | 문자열 | ✅ | "네" / "아니오, 체크카드만 사용해요." |
| 최근 3개월 월평균 신용카드 사용금액 | 신용카드 월평균 | 정수 | 조건부 | 신용카드=네 일 때만 |
| 최근 3개월 월평균 체크카드 사용금액 | 체크카드 월평균 | 정수 | 조건부 | 신용카드=아니오(체크카드만) 일 때만 |
| 현금서비스 이용 여부 | 이용 여부 | 문자열 | ✅ | "네, 지금 이용중이에요." / "아니오, 안쓰고 있어요." |
| 현금서비스 이용금액 | 현금서비스 금액 | 정수 | 조건부 | 현금서비스=네 일 때만 |
| 휴대폰 소액결제 이용 여부 | 이용 여부 | 문자열 | ✅ | "네" / "아니오" |
| 최근 3개월 월 소액결제 금액 | 소액결제 금액 | 정수 | 조건부 | 소액결제=네 일 때만 |
| 최근 3개월 월평균 휴대폰 요금 | 휴대폰 요금 | 정수 | ✅ | **소액결제 여부 무관, 항상 필수** |

### 3-3. 금융이력

| 항목명 (탈리 문구) | 필드 설명 | 타입 | 필수 | 비고 |
|-------------------|----------|------|------|------|
| NICE 기준 신용점수 | NICE 점수 | 정수 | ✅ | 0~1000 |
| KCB 기준 신용점수 | KCB 점수 | 정수 | ✅ | 0~1000 |
| 현재 대출 보유 여부 | 보유 여부 | 문자열 | ✅ | "네" / "아니오" |
| 총 대출 잔액 | 총 잔액 | 정수 | 조건부 | 대출보유=네 일 때만, 단위: 만 원 |
| 대출기관별 상세 | 기관명/최초금액/잔액 자유 텍스트 | TEXT | 조건부 | 대출보유=네 일 때만, 자유 입력 |
| 제도권 해당 여부 | 해당 제도 | 문자열(다중) | ✅ | 신용회복위원회 / 개인회생 / 워크아웃 / 새출발기금 / 채무조정 / 해당 안해요. |

### 3-4. 증빙서류 (파일 업로드)

| 항목명 | 설명 | 필수 | 최대 파일 수 | 비고 |
|--------|------|------|-------------|------|
| 건강보험 자격득실확인서 | 건강보험 증빙 이미지 | ❌ 선택 | 3개 | 이미 카톡/앱 제출 시 생략 가능 |
| 신용정보 화면 캡처 | 토스 또는 카카오페이 신용정보 | ✅ 필수 | 4개 이상 | 토스 최대 5장 / 카카오페이 최대 6장 |
| 채무변제 증빙서류 | 변제내역 확인서 캡처 | 조건부 | 3개 | 제도권(개인회생/워크아웃/새출발기금/채무조정) 해당 시만 |

---

## ⚠️ API 작업 전 필독 사항 (서버팀 준수 요청)

### 1. 기존 API URL 침범 금지

이 프로젝트에서 신규로 생성하는 API 엔드포인트는 **기존 써주세요 서버의 API URL 체계와 충돌하지 않도록** 반드시 확인 후 설계해주세요.
신규 엔드포인트 URL 확정 전에 기존 API 목록을 먼저 검토하여 경로 중복 또는 네이밍 충돌이 없는지 검증해주세요.

### 2. API 정책 페이퍼 선검토 필수

서버팀 워크스페이스 `project-docs/90_API_Information/` 폴더에 아래 4가지 문서가 있습니다.
**API 신규 작업 착수 전 반드시 이 문서들을 먼저 확인해주세요.**

| 파일명 | 용도 |
|--------|------|
| `API_DOMAIN_LIST_20260426.md` | 써주세요 API 도메인 목록 |
| `API_ENDPOINT_LIST_20260426.md` | 기존 전체 엔드포인트 목록 |
| `CLIENT_API_CALL_LIST_20260426.md` | 클라이언트 API 호출 목록 |
| `SERVER_TEAM_DOMAIN_CHANGE_CONTROL_NOTICE_20260426.md` | 도메인 변경 통제 공지 |

> 위 문서 기준으로 신규 엔드포인트 URL을 설계하고, 기존 체계와 일관성을 유지해주세요.

---

## 4. API 요구사항 (⚠️ 엔드포인트 URL은 서버팀 결정)

> ⚠️ 아래는 **기능 요구사항**입니다. 실제 URL, 파라미터명, 응답 구조는 서버팀이 확정한 후 쮸리팀에 전달해주세요. 쮸리팀은 서버팀 확정 스펙 수령 후에만 API 연동 코드를 작성합니다.

### 4-1. 필요한 API 기능 목록

| # | 기능 | 방향 | 비고 |
|---|------|------|------|
| A | 4차 심사 텍스트/숫자 데이터 제출 | 고객 → 서버 | 아래 상세 참조 |
| B | 파일 업로드 | 고객 → S3 | **방식은 서버팀 결정** |
| C | 4차 심사 목록 조회 | 어드민 → 서버 | 필터/페이지네이션 |
| D | 4차 심사 상세 조회 | 어드민 → 서버 | 파일 URL 포함 |
| E | 파일 다운로드 | 어드민 → S3 | presigned URL 또는 proxy |
| F | 심사 상태 변경 | 어드민 → 서버 | 접수/검토중/완료/반려 |

### 4-2. API A — 텍스트/숫자 데이터 제출 (요구사항)

**요청 방향**: 고객 브라우저 → 서버
**요청 방식**: HTTP POST, Content-Type: `application/json`

---

#### 📋 필드명 제안 테이블 (쮸리팀 제안 → 서버팀 확정/수정 후 회신)

> ⚠️ 아래 필드명은 쮸리팀이 제안한 **초안**입니다. 서버팀이 기존 API 네이밍 규칙에 맞게 수정하여 확정 회신해주세요.

##### 기본정보

| 필드명 (제안) | 타입 | 필수 | 예시값 | 비고 |
|--------------|------|------|--------|------|
| `name` | string | ✅ | `"홍길동"` | 한글 2~5자 |
| `birth_date` | string | ✅ | `"1990.05.15"` | YYYY.MM.DD 형식 |
| `phone` | string | ✅ | `"01012345678"` | 숫자만, 01X 시작 10~11자리 |

##### 소득과 지출

| 필드명 (제안) | 타입 | 필수 | 예시값 | 비고 |
|--------------|------|------|--------|------|
| `annual_income` | integer | ✅ | `3600` | 단위: 만 원 |
| `max_monthly_income` | integer | ✅ | `400` | 1년 중 월 최고 소득, 단위: 만 원 |
| `fixed_monthly_income` | integer | ✅ | `300` | 월 고정 소득, 단위: 만 원 |
| `max_monthly_expense` | integer | ✅ | `250` | 1년 중 월 최고 지출, 단위: 만 원 |
| `fixed_monthly_expense` | integer | ✅ | `180` | 월 고정 지출, 단위: 만 원 |
| `uses_credit_card` | boolean | ✅ | `true` | 신용카드 사용 여부 |
| `avg_credit_card_monthly` | integer | 조건부 | `50` | `uses_credit_card=true` 일 때만, 단위: 만 원 / 미사용 시 `null` |
| `avg_debit_card_monthly` | integer | 조건부 | `30` | `uses_credit_card=false` 일 때만, 단위: 만 원 / 미해당 시 `null` |
| `uses_cash_advance` | boolean | ✅ | `false` | 현금서비스 이용 여부 |
| `cash_advance_amount` | integer | 조건부 | `100` | `uses_cash_advance=true` 일 때만, 단위: 만 원 / 미사용 시 `null` |
| `uses_micropayment` | boolean | ✅ | `true` | 휴대폰 소액결제 이용 여부 |
| `micropayment_monthly_amount` | integer | 조건부 | `10` | `uses_micropayment=true` 일 때만, 단위: 만 원 / 미사용 시 `null` |
| `phone_bill_monthly` | integer | ✅ | `5` | 월 휴대폰 요금, 단위: 만 원 (소액결제 무관 항상 필수) |

##### 금융이력

| 필드명 (제안) | 타입 | 필수 | 예시값 | 비고 |
|--------------|------|------|--------|------|
| `credit_score_nice` | integer | ✅ | `750` | NICE 신용점수, 범위 0~1000 |
| `credit_score_kcb` | integer | ✅ | `730` | KCB 신용점수, 범위 0~1000 |
| `has_existing_loan` | boolean | ✅ | `true` | 현재 대출 보유 여부 |
| `total_loan_balance` | integer | 조건부 | `2000` | `has_existing_loan=true` 일 때만, 단위: 만 원 / 미해당 시 `null` |
| `loan_details` | string | 조건부 | `"새마을금고 최초 1000만원 잔액 800만원\n카카오뱅크 최초 500만원 잔액 200만원"` | `has_existing_loan=true` 일 때만, 자유 텍스트 / 미해당 시 `null` |
| `debt_adjustment_types` | array of string | ✅ | `["개인회생", "워크아웃"]` 또는 `["해당 안해요"]` | 제도권 해당 여부 다중 선택, 값 목록: `"신용회복위원회"` / `"개인회생"` / `"워크아웃"` / `"새출발기금"` / `"채무조정"` / `"해당 안해요"` |

---

#### 📦 제안 Request JSON 전체 예시

```json
{
  "name": "홍길동",
  "birth_date": "1990.05.15",
  "phone": "01012345678",

  "annual_income": 3600,
  "max_monthly_income": 400,
  "fixed_monthly_income": 300,
  "max_monthly_expense": 250,
  "fixed_monthly_expense": 180,
  "uses_credit_card": true,
  "avg_credit_card_monthly": 50,
  "avg_debit_card_monthly": null,
  "uses_cash_advance": false,
  "cash_advance_amount": null,
  "uses_micropayment": true,
  "micropayment_monthly_amount": 10,
  "phone_bill_monthly": 5,

  "credit_score_nice": 750,
  "credit_score_kcb": 730,
  "has_existing_loan": true,
  "total_loan_balance": 2000,
  "loan_details": "새마을금고 최초 1000만원 잔액 800만원\n카카오뱅크 최초 500만원 잔액 200만원",
  "debt_adjustment_types": ["개인회생", "워크아웃"]
}
```

> 📝 **서버팀 확인 요청 포인트**
> 1. **필드명 네이밍**: snake_case 제안 → 기존 API 규칙(camelCase 등)에 맞게 수정 가능
> 2. **boolean 필드**: `true/false` 제안 → `"Y"/"N"` 또는 `1/0` 방식도 가능, 서버팀 결정
> 3. **조건부 null 처리**: 미해당 필드는 `null`로 전송 제안 → 키 자체를 생략(omit)할지 서버팀 결정
> 4. **`debt_adjustment_types`**: array 제안 → comma-separated string `"개인회생,워크아웃"` 방식도 가능, 서버팀 결정
> 5. **단위 통일**: 전체 금액 필드를 만 원 단위 정수로 제안 → 원(₩) 단위로 바꿀지 서버팀 결정

---

**응답으로 필요한 것**:
- 성공/실패 여부
- 성공 시: 접수 번호 또는 레코드 ID (파일 업로드 API에서 참조용)

```json
// 성공 응답 예시 (서버팀 확정 필요)
{
  "success": true,
  "application_id": 1042,
  "receipt_number": "2026-04-R-1042"
}
```

### 4-3. API B — 파일 업로드 (방식 서버팀 결정 필요)

**⚠️ 쮸리팀은 서버팀이 결정한 방식에 맞춰 프론트 코드를 작성합니다.**

| 방식 | 설명 | 서버팀 결정 필요 항목 |
|------|------|----------------------|
| **A안**: multipart/form-data | 서버가 파일 수신 후 S3 저장 | 엔드포인트, 파일 필드명 |
| **B안**: S3 presigned URL | 클라이언트가 S3에 직접 업로드 | presigned URL 발급 API, S3 버킷 CORS 설정 |

**파일 업로드 시 함께 전달해야 할 정보**:
- 4-2에서 발급받은 접수 ID (어느 신청 건의 파일인지 연결)
- 파일 종류 구분 (건강보험증빙 / 신용정보캡처 / 채무변제증빙)

**파일 제한 (서버팀 정책 결정 필요)**:
- 허용 확장자: JPG, PNG, PDF (최소한 이 3가지)
- 파일 최대 크기: 서버팀 결정
- 총 업로드 가능 파일 수: 최대 10개 (3+4+3)

---

## 5. DB 스키마 요구사항 (서버팀 설계 — 아래는 요구사항 가이드)

> 아래는 저장이 필요한 데이터 목록입니다. 실제 테이블명/컬럼명/타입은 서버팀이 결정해주세요.

### 5-1. 4차 심사 신청 테이블 (예: `review_applications`)

| 데이터 | 타입 권고 | 필수 | 비고 |
|--------|----------|------|------|
| 고유 ID | BIGINT PK | ✅ | Auto increment |
| 접수 번호 | VARCHAR | ✅ | 예: 2026-04-R-0001 |
| **연결 키**: 1차 신청 ID | BIGINT (FK) | ❓ | 1차 신청(loan_applications)과 연결 여부 서버팀 판단 |
| 성함 | VARCHAR | ✅ | 암호화 저장 권고 |
| 생년월일 | VARCHAR | ✅ | YYYY.MM.DD 형식, 암호화 저장 권고 |
| 휴대폰번호 | VARCHAR | ✅ | 암호화 저장 권고 |
| 연소득 | INT | ✅ | 만 원 단위 |
| 1년중 월최고소득 | INT | ✅ | |
| 월고정소득 | INT | ✅ | |
| 1년중 월최고지출 | INT | ✅ | |
| 월고정지출 | INT | ✅ | |
| 신용카드 사용 여부 | VARCHAR | ✅ | |
| 신용카드 월평균 | INT | - | nullable |
| 체크카드 월평균 | INT | - | nullable |
| 현금서비스 이용 여부 | VARCHAR | ✅ | |
| 현금서비스 이용금액 | INT | - | nullable |
| 소액결제 이용 여부 | VARCHAR | ✅ | |
| 소액결제 금액 | INT | - | nullable |
| 휴대폰 요금 | INT | ✅ | |
| NICE 신용점수 | INT | ✅ | |
| KCB 신용점수 | INT | ✅ | |
| 대출 보유 여부 | VARCHAR | ✅ | |
| 총 대출 잔액 | INT | - | nullable |
| 대출기관 상세 | TEXT | - | nullable, 자유 텍스트 |
| 제도권 해당 여부 | VARCHAR/TEXT | ✅ | 다중 선택값 저장 방식 서버팀 결정 |
| 심사 상태 | ENUM | ✅ | 접수/검토중/완료/반려 (default: 접수) |
| 관리자 메모 | TEXT | - | nullable |
| 접수 일시 | DATETIME | ✅ | |
| 수정 일시 | DATETIME | ✅ | |

### 5-2. 파일 메타데이터 테이블 (예: `review_files`)

| 데이터 | 타입 권고 | 필수 | 비고 |
|--------|----------|------|------|
| 고유 ID | BIGINT PK | ✅ | |
| 신청 ID | BIGINT (FK) | ✅ | review_applications.id 참조 |
| 파일 종류 | ENUM | ✅ | 건강보험증빙 / 신용정보캡처 / 채무변제증빙 |
| S3 키 (경로) | VARCHAR | ✅ | S3 내 파일 위치 |
| 원본 파일명 | VARCHAR | ✅ | 고객이 업로드한 파일명 |
| 파일 크기 | BIGINT | ✅ | bytes |
| MIME 타입 | VARCHAR | ✅ | image/jpeg, image/png, application/pdf |
| 업로드 일시 | DATETIME | ✅ | |

---

## 6. 어드민 패널 요구사항

써주세요 기존 어드민 모드에 아래 기능을 추가해주세요.

### 6-1. 4차 심사 목록 화면

| 기능 | 설명 |
|------|------|
| 목록 조회 | 접수일시, 성함, 연락처, 심사 상태 표시 |
| 필터 | 날짜 범위 / 심사 상태 |
| 페이지네이션 | 20건씩 |
| 상세 보기 버튼 | 각 행에서 상세 화면으로 이동 |

**목록 컬럼 (최소)**:
```
접수번호 | 접수일시 | 성함 | 휴대폰 | NICE점수 | KCB점수 | 상태 | 상세보기
```

### 6-2. 4차 심사 상세 화면

| 기능 | 설명 |
|------|------|
| 전체 데이터 조회 | 3-1 ~ 3-3 모든 텍스트/숫자 데이터 표시 |
| 파일 목록 | 업로드된 파일 목록 (파일명, 종류, 업로드일시) |
| 파일 다운로드 | 각 파일 개별 다운로드 버튼 |
| 전체 파일 일괄 다운로드 | ZIP으로 묶어서 다운로드 |
| 심사 상태 변경 | 접수 → 검토중 → 완료 / 반려 |
| 관리자 메모 | 입력/수정/저장 |

---

## 7. 보안 요구사항

| 항목 | 요구사항 |
|------|----------|
| 전송 | HTTPS 필수 (개인정보 + 파일 전송) |
| 인증 | 어드민 API는 인증 없이 접근 불가 |
| 개인정보 암호화 | 성함, 생년월일, 휴대폰번호 컬럼 암호화 저장 |
| S3 접근 제어 | S3 버킷 퍼블릭 접근 차단, presigned URL로만 접근 |
| CORS | `apply-4차.html` 서빙 도메인만 허용 |
| 파일 검증 | 서버 측 파일 타입 검증 (확장자만 아닌 MIME 타입 체크) |
| 파일 크기 제한 | 서버팀 정책 결정 후 서버/클라이언트 양측 적용 |
| Rate Limiting | 동일 IP 분당 제출 횟수 제한 (스팸 방지) |

---

## 8. 역할 분담 요약

| 항목 | 담당 | 상태 |
|------|------|------|
| 4차 심사 폼 HTML+JS (`apply-4차.html`) | 쮸리팀 | ⏳ API 확정 후 개발 |
| 조건부 로직 21개 Rule 구현 | 쮸리팀 | ⏳ API 확정 후 개발 |
| 파일 업로드 UI | 쮸리팀 | ⏳ API 확정 후 개발 |
| 폼 → API 연동 코드 | 쮸리팀 | ⏳ **B-5 API URL 수령 후에만 시작** |
| API A (텍스트 데이터 제출) | **서버팀** | ❌ 미착수 |
| API B (파일 업로드, 방식 결정 포함) | **서버팀** | ❌ 미착수 |
| API C/D (어드민 목록/상세) | **서버팀** | ❌ 미착수 |
| API E (파일 다운로드) | **서버팀** | ❌ 미착수 |
| DB 스키마 설계 및 생성 | **서버팀** | ❌ 미착수 |
| S3 버킷 생성 및 정책 설정 | **서버팀** | ❌ 미착수 |
| 어드민 패널 기능 추가 | **서버팀** | ❌ 미착수 |
| AWS 배포 | **서버팀** | ❌ 미착수 |

---

## 9. 서버팀 작업 우선순위 (권고)

```
1순위: 파일 업로드 방식 결정 (A안: multipart / B안: presigned URL)
       + API A/B 엔드포인트 URL 및 스펙 확정
       → 확정 즉시 쮸리팀에 아래 내용 전달:
          ① API URL (DEV / PRD 각각)
          ② Request 필드명 전체
          ③ 파일 업로드 방식 및 사용법
          ④ 파일 허용 확장자 / 최대 크기

2순위: DB 스키마 + S3 버킷 생성
       + API A/B 구현

3순위: 어드민 패널 목록/상세/다운로드 기능 추가

4순위: 상태 변경, 메모, ZIP 다운로드 등 고도화
```

---

## 10. 참고 링크

- 1차 신청 폼 (PRD): `monthly-loan.sirjuseyo.com/2026-04/apply.html`
- 1차 신청 폼 (DEV): `monthly-loan.sirjuseyo.com/2026-04/apply-dev.html`
- 탈리 4차 심사 폼 (참고용): `https://tally.so/r/mBvDvR` (탈리 대시보드 로그인 필요)
- GitHub 레포: `https://github.com/sirjuseyo/monthly-loan`
- 기획서: `PLAN_4차심사자료_내재화기획서.md` (워크스페이스 폴더)

| 구분 | 실제 접속 URL | 단축 URL |
|------|------------------------------|------|
| PRD 폼 | `https://monthly-loan.sirjuseyo.com/apply-review/apply-review.html` | https://buly.kr/GEA7YaO |
| DEV 폼 | `https://monthly-loan.sirjuseyo.com/apply-review/apply-review-dev.html` | - |
| 정적 목업 | `https://monthly-loan.sirjuseyo.com/apply-review/apply-review-mockup.html` | - |

---

## 11. 쮸리팀 → 서버팀 전달 요청 사항 (회신 필요)

서버팀에서 아래 항목을 확정해서 회신해주시면 쮸리팀이 즉시 HTML 연동 코드 작업을 시작합니다.

```
① 파일 업로드 방식: A안(multipart) / B안(presigned URL) 중 선택
② API 엔드포인트 URL (DEV / PRD 각각)
③ 텍스트 데이터 제출 Request payload 필드명 전체
④ 파일 업로드 Request 방법 (필드명, 헤더 등)
⑤ 파일 허용 확장자 / 파일당 최대 크기
⑥ 성공 Response 구조 (특히 접수 ID 형식)
```

> 문의사항은 댄디어빠쮸너야님을 통해 쮸리팀으로 전달해주세요.
